---
const tier = Astro.url.searchParams.get('tier');
const dest = tier ? `/profile/new?mode=signup&tier=${encodeURIComponent(tier)}` : '/profile/new?mode=signup';
return Astro.redirect(dest, tier ? 302 : 301);
---
