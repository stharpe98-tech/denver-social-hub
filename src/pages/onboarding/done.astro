---
// Onboarding is now a single-screen flow that lands users directly at
// /events. This page is kept as a redirect so old bookmarks (or emails
// that referenced the multi-step flow) still resolve.
return Astro.redirect('/events', 301);
---
